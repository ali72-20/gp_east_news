import 'package:flutter/cupertino.dart';

class items_list extends StatelessWidget {
  const items_list({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
